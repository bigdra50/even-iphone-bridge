# XcodeGen 生成物 (project.yml が真。.xcodeproj はコミットしない)
*.xcodeproj/

# Xcode / SPM
.build/
DerivedData/
*.xcworkspace/xcuserdata/
xcuserdata/
.swiftpm/
Package.resolved

# CI で固定パスに clone する SPM 依存
.spm/

# CI で固定パスに clone する SPM 依存
.spm/

.DS_Store
